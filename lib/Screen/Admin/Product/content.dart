import 'package:flutter/material.dart';

class ProductWebContent extends StatelessWidget {
  const ProductWebContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Products'),
      ),
    );
  }
}
